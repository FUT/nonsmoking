- if can? :edit, diary
  h1.center Тест Фагестрома

  p.muted Инструкция:
  p.muted Внимательно прочтите каждый вопрос и выберите один из предложенных вариантов ответа.

  = simple_form_for :fagerstrom, url: fagerstrom_diary_path(@diary) do |f|
    ol
      li = f.input :q1, label: 'Как скоро вы выкуриваете первую сигарету, после пробуждения?',
                        collection: [\
                          ['Не ранее, чем через 60 минут', 0],\
                          ['31-60 минут', 1],\
                          ['6-30 минут', 2],\
                          ['В течение 5 минут', 3]\
                        ], as: :radio_buttons

      li = f.input :q2, label: 'Трудно ли вам воздержаться от курения там, где это запрещено?',
                        collection: [\
                          ['Нет', 0],\
                          ['Да', 1]\
                        ], as: :radio_buttons

      li = f.input :q3, label: 'Отказ от курения какой сигареты вызывает у вас наибольшее неприятие?',
                        collection: [\
                          ['Первой утренней', 1],\
                          ['Любой другой', 0]\
                        ], as: :radio_buttons

      li = f.input :q4, label: 'Сколько сигарет в день вы выкуриваете?',
                        collection: [\
                          ['10 или меньше', 0],\
                          ['11-20', 1],\
                          ['21-30', 2],\
                          ['31 или больше', 3]\
                        ], as: :radio_buttons

      li = f.input :q5, label: 'В первые часы после пробуждения вы курите чаще, чем в оставшуюся часть дня?',
                        collection: [\
                          ['Нет', 0],\
                          ['Да', 1]\
                        ], as: :radio_buttons

      li = f.input :q6, label: 'Курите ли вы, если настолько больны, что вынуждены оставаться в постели большую часть дня?',
                        collection: [\
                          ['Нет', 0],\
                          ['Да', 1]\
                        ], as: :radio_buttons

    .center = f.submit 'Продолжить', class: 'btn btn-large btn-success'
- else
  = render 'not_finished'
